[project]
name = "dictionary-lookup-mcp"
version = "0.1.0"
description = "A simple MCP server that defines English words"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.28,<2",
    "httpx>=0.27",
]
